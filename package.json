{
  "name": "use-lang-direction",
  "version": "0.1.8",
  "description": "A React hook that reads an element's dir attribute value and any updates to that value",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "build": "yarn build:clean && yarn build:pkg && yarn copy",
    "build:clean": "rimraf dist",
    "build:pkg": "babel src --out-dir dist",
    "copy": "yarn copy:pkg & yarn copy:readme",
    "copy:pkg": "babel package.json --out-dir dist --copy-files",
    "copy:readme": "babel README.md --out-dir dist --copy-files"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/davidicus/use-lang-direction.git"
  },
  "keywords": [
    "React",
    "React",
    "hooks",
    "BIDI",
    "ltr",
    "rtl",
    "i18n"
  ],
  "author": "David Conner <david@david-conner.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/davidicus/use-lang-direction/issues"
  },
  "homepage": "https://github.com/davidicus/use-lang-direction#readme",
  "peerDependencies": {
    "react": ">=16.8.0",
    "react-dom": ">=16.8.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.6",
    "@babel/preset-env": "^7.9.6",
    "@babel/preset-react": "^7.9.4",
    "babel-jest": "^26.0.1",
    "enzyme": "^3.11.0",
    "enzyme-adapter-react-16": "^1.15.2",
    "jest": "^26.0.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "rimraf": "^3.0.2"
  }
}
